clone repo with SSH --> not in container!
open repo
open folder in container --> root --> select config file
if login context error --> direkt per RDP
open corresponding workspace

backend:
dotnet restore
run task "run" (dotnet run) or "watch"
listens on Kestrel default (http://localhost:5000); to reach it from the
frontend container bind to all interfaces, e.g.
  dotnet run --project Project.csproj --urls http://0.0.0.0:5000


frontend:
dotnet restore
dotnet dev-certs https --trust

adjust settings in appsettings.Development.json
BackendBaseUrl http://172.17.0.3:5000 (adjust for backend host/IP and port)
ClientId dd0f61d4-5801-402d-8d75-a8e74af8d681
TenantId 92f4dd01-f0ea-4b5f-97f2-505c2945189c
ClientSecret ...

hit F5

open https://localhost:7100